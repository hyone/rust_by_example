fn main() {
    let logical: bool = true;

    let a_float: f64 = 1.0;
    let an_integer   = 5132;

    let default_float   = 3.0;  // default type is f64
    let default_integer = 7;    // default type is i32

    let mut mutable = 12; // Mutable `i32` .

    mutable = 33;

    // // Error! the type of variable can't be changed
    // mutable = true;
}
